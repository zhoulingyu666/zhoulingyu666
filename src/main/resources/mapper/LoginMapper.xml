<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.crud_vue.mapper.LoginMapper">

    <resultMap id="BaseResultMap" type="com.example.crud_vue.entity.Login">
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="username" jdbcType="VARCHAR" property="username" />
        <result column="password" jdbcType="VARCHAR" property="password" />
    </resultMap>

    <sql id="Base_Column_List">
        id, username, password
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/>
        from login
        where id = {#id,jdbcType=INTEGER}
    </select>
    <select id="selectByUsernameAndPassword" resultType="com.example.crud_vue.entity.Login">
        select <include refid="Base_Column_List" />
        from login where username = #{username} and password = #{password}
    </select>
</mapper>